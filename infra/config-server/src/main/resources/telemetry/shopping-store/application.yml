server:
  port: 0

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/shoppingstore
    username: postgres
    password: password
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect